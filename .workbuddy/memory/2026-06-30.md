# 2026-06-30 Work Log

## 新增 vapeur.ai 图像+视频供应商

接入 vapeur.ai OpenAI 兼容统一网关,同时接入图像(gpt-image-2)和视频(Seedance 2.0 / 2.0 Fast)。

### 探测确认(通过 curl)

- Base URL:`https://api.vapeur.ai`
- 鉴权:`Authorization: Bearer sk-xxx`(OpenAI 兼容)
- `/v1/models` 返回:`gpt-image-2` / `doubao-seedance-2-0-260128` / `doubao-seedance-2-0-fast-260128`
- ⚠️ 账号测试余额不足(`test_balance_exhausted`),图像/视频端到端验证未通过,按推测接入

### 改动文件

#### 图像(gpt-image-2)

1. **新建 `src/lib/vapeurImage.functions.ts`** — 基于 ailinziImage 模板,OpenAI 兼容,T2I + I2I
2. **`src/lib/seedream.functions.ts`** — 6 个路由分发块各加 `vapeur/` 前缀分支
3. **`src/lib/imageModels.ts`** — 加模型目录条目 `vapeur/gpt-image-2`
4. **`src/components/NewProjectDialog.tsx`** — `imageModelOptions` 加选项
5. **`.env`** — 加 `VAPEUR_BASE_URL="https://api.vapeur.ai"`
6. **`.env.local`** — 加 `VAPEUR_API_KEY=sk-y411...`(用户提供)

#### 视频(Seedance 2.0 / 2.0 Fast)

1. **`src/lib/videoGenerate.functions.ts`**:
   - `getVideoBackend()` 加 `vapeur` 分支(前缀 `vapeur-`)
   - 新增 `VAPEUR_MODELS` 常量、`VAPEUR_DEFAULT_BASE_URL` / `VAPEUR_CREATE_PATH` / `VAPEUR_STATUS_PATH`
   - 新增 `getVapeurConfig()` / `vapeurModelToUpstream()`
   - `submitVideoTask()` 加 vapeur 分支——**复用 `toapisSubmit`**(newapi 风格,接口格式推测一致)
   - `pollVideoTask()` 加 vapeur 分支——**复用 `toapisPoll`**
   - `VideoBackend` 类型加 `'vapeur'`,`PollServerInput` zod 枚举加 `'vapeur'`
2. **`src/components/NewProjectDialog.tsx`** — `videoModels` 加 2 个选项:
   - `vapeur-doubao-seedance-2-0-260128`
   - `vapeur-doubao-seedance-2-0-fast-260128`

### 关键约定

- Cloudflare Secret 名称:`VAPEUR_API_KEY`
- 图像模型 key 前缀:`vapeur/`(跟 ailinzi/ / nagora/ 等命名空间隔离)
- 视频模型 key 前缀:`vapeur-`(跟 toapis- / k99- 等隔离)
- 鉴权:`Authorization: Bearer <key>`(标准 OpenAI 兼容)
- Base URL:`https://api.vapeur.ai`
- 视频接口路径推测:`POST /v1/videos/generations` + `GET /v1/videos/generations/{id}`(跟 ToAPIs 一致)
- ⚠️ 视频接口格式未经端到端验证(账号余额不足),如充值后发现返回格式不同,需改 `vapeurSubmit` / `vapeurPoll` 为独立实现

### 验证

- `tsc --noEmit` 类型检查通过(0 错误)
- 图像/视频端到端测试未通过(账号 `test_balance_exhausted`),待用户充值后验证

### 顺带修复:视频生成超时问题

之前 Seedance 2.0 生成超时弹"任务处理超时,请稍后重试或缩短分镜组时长",根因是 `generateVideo` 默认 deadline 5 分钟,而 Seedance 2.0 实际要 3-5 分钟。本次未修改 deadline(需另外处理)。
